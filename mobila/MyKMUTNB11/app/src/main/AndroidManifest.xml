<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.mykmutnb11">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MyKmutnb"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CheckGradeActivity" />
        <activity android:name=".SubjectTableActivity" />
        <activity android:name=".TestTableActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".MyinfoActiviti"></activity>
    </application>

</manifest>